rabbit_jobs:
  url: amqp://example.com/vhost
  prefix: 'rabbit_jobs'
  queues:
    durable_queue:
      durable: true
      auto_delete: false
      ack: true
      arguments:
        x-ha-policy: all
    fast_queue:
      durable: false
      auto_delete: true
      ack: false